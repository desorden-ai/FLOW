name: Execute DESORDEN update
run-name: Apply accumulated DESORDEN changes and deploy

on:
  push:
    branches: [main]
    paths:
      - .desorden-update-trigger

permissions:
  contents: write
  statuses: write

jobs:
  apply-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Mark update pending
        uses: actions/github-script@v8
        with:
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: "pending",
              context: "desorden/apply",
              description: "Applying accumulated DESORDEN changes",
              target_url: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });

      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Reconstruct media and apply prepared update
        id: apply
        run: |
          cat .desorden-assets/chunk-* | base64 --decode > /tmp/desorden-assets.zip
          mkdir -p public/media
          unzip -q -o /tmp/desorden-assets.zip -d public/media
          test -s public/media/hero/portada-chico-bn.webp
          test -s public/media/social-proof/rosalia.webp
          test -s public/media/social-proof/rozalen.webp
          test -s public/media/social-proof/leire.webp

          python3 - <<'PY'
          from pathlib import Path
          import base64
          import os
          import subprocess

          source = Path('.github/workflows/apply-desorden-update.yml').read_text(encoding='utf-8')
          prefix = 'APPLY_SCRIPT_B64:'
          line = next((item.strip() for item in source.splitlines() if item.strip().startswith(prefix)), None)
          if line is None:
              raise SystemExit('Missing encoded patch script')
          encoded_script = line.split(':', 1)[1].strip().strip('"').strip("'")

          script = Path('/tmp/apply_desorden.py')
          script.write_bytes(base64.b64decode(encoded_script))

          files = {
              'HERO_B64': Path('public/media/hero/portada-chico-bn.webp'),
              'ROSALIA_B64': Path('public/media/social-proof/rosalia.webp'),
              'ROZALEN_B64': Path('public/media/social-proof/rozalen.webp'),
              'LEIRE_B64': Path('public/media/social-proof/leire.webp'),
          }
          env = os.environ.copy()
          env.update({name: base64.b64encode(path.read_bytes()).decode('ascii') for name, path in files.items()})
          subprocess.run(['python3', str(script)], check=True, env=env)

          page = Path('app/page.tsx')
          content = page.read_text(encoding='utf-8')
          content = content.replace('width={1147}', 'width={768}', 1).replace('height={1536}', 'height={1028}', 1)
          page.write_text(content, encoding='utf-8')
          PY

      - name: Install dependencies
        id: install
        run: npm ci

      - name: Test
        id: test
        run: npm test

      - name: Build
        id: build
        run: npm run build

      - name: Commit integrated changes
        id: commit
        run: |
          git config user.name "DESORDEN Deploy Bot"
          git config user.email "actions@users.noreply.github.com"
          git add app/page.tsx app/globals.css tests/rendered-html.test.mjs public/media/hero/portada-chico-bn.webp public/media/social-proof/rosalia.webp public/media/social-proof/rozalen.webp public/media/social-proof/leire.webp
          git commit -m "feat: add DESORDEN hero visuals and artist social proof"
          git push origin HEAD:main

      - name: Verify Cloudflare credentials
        id: credentials
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          test -n "$CLOUDFLARE_API_TOKEN"
          test -n "$CLOUDFLARE_ACCOUNT_ID"

      - name: Deploy updated site to Cloudflare Workers
        id: deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: "4.92.0"
          command: deploy

      - name: Publish update result
        if: always()
        uses: actions/github-script@v8
        env:
          APPLY_OUTCOME: ${{ steps.apply.outcome }}
          INSTALL_OUTCOME: ${{ steps.install.outcome }}
          TEST_OUTCOME: ${{ steps.test.outcome }}
          BUILD_OUTCOME: ${{ steps.build.outcome }}
          COMMIT_OUTCOME: ${{ steps.commit.outcome }}
          CREDENTIALS_OUTCOME: ${{ steps.credentials.outcome }}
          DEPLOY_OUTCOME: ${{ steps.deploy.outcome }}
        with:
          script: |
            const phases = [
              ["apply", process.env.APPLY_OUTCOME],
              ["install", process.env.INSTALL_OUTCOME],
              ["tests", process.env.TEST_OUTCOME],
              ["build", process.env.BUILD_OUTCOME],
              ["commit", process.env.COMMIT_OUTCOME],
              ["credentials", process.env.CREDENTIALS_OUTCOME],
              ["deployment", process.env.DEPLOY_OUTCOME],
            ];
            const failed = phases.find(([, outcome]) => outcome && outcome !== "success" && outcome !== "skipped");
            const success = process.env.DEPLOY_OUTCOME === "success";
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: success ? "success" : "failure",
              context: "desorden/apply",
              description: success ? "DESORDEN changes applied and deployed" : `DESORDEN update failed${failed ? ` during ${failed[0]}` : ""}`,
              target_url: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
